# 2026-07-11

## 核查 GLM-5.2-FP8 架构指标（serve_model_arch.py 输出审计）

用户贴出的四个指标（参数量 4541.22B / 激活 159.95B / KV cache 3744 MiB / FLOPs 319.9 GFLOPs）经核对全部来自 `serve_model_arch.py` line 1164-1184 的通用估算公式，且与公式输出逐一吻合，但**对该 MLA+MoE 架构均有误**：

- 真实总参数量 ≈ 755.6B（依据 safetensors.index.json `total_size=755,617,140,416` 字节，模型为 FP8 e4m3，权重约 1 字节/参数）。
- 真实激活 ≈ 54.6B；真实 FLOPs ≈ 109 GFLOPs/token；真实 KV cache ≈ 43.8 MiB(FP8)/87.7 MiB(BF16) 每 1K token。

### 三个根因 bug（在 serve_model_arch.py 估算段）
1. MoE 专家维度用错：FFN 单元取 `intermediate_size=12288`（稠密/共享专家维度）却 ×256 路由专家；真实 `moe_intermediate_size=2048`，比值 6×，导致总参数量被整体放大 6×。
2. KV cache 用 GQA 公式 `2·L·kv_heads·head_dim·2B`，把 MLA 当全量 GQA；MLA 实际只缓存压缩 latent `kv_lora(512)+rope_dim(64)=576`/层，比真值高 43–85×。最危险，会严重误导显存规划。
3. 激活参数未区分前 3 层稠密 FFN（`first_k_dense_replace=3`），假定全层都是 top-8/256 MoE。

### 修正已实施（serve_model_arch.py）
改动三处：
1. 新增 config 解析 `moe_intermediate_size`(→moe_ffn_hidden)、`n_shared_experts`、`first_k_dense_replace`。
2. 修正 `qk_nope_head_dim` 改为优先读 config（原来错误地用 head_dim-qk_rope=128，实际 config=192），并新增 `qk_head_dim`。
3. 重写估算段（原 line 1177-1198）：MLA 走低秩投影公式算注意力参数；路由/共享专家用 moe_intermediate_size；区分前 first_k_dense 稠密层；MLA 的 KV cache 只缓存压缩 latent (kv_lora+rope)*bf16。

修正后工具对 GLM-5.2-FP8 输出：**743.06B (active 39.98B) / KV cache 87.8 MB / 80.0 GFLOPs**。
关键修正：共享专家维度用 moe_intermediate_size=2048（非初判的 12288）；checkpoint 专家层为 layer 3–78 共 76 层，其中 layer 78 是 MTP 层，主干按 78 层(3稠密+75 MoE)计，与字节数 755.6GB 自洽。

回归验证：DeepSeek-V4-Pro(MLA) 1592.84B/68.69B、Mistral-Medium(dense) 125B、Keye-VL-30B-A3B active 2.63B(与命名 A3B 吻合，强旁证)。MiniMax-M3 config 字段命名不同返回空（非本次改动导致）。

报告文件：D:\code\github\model_arch_view\GLM-5.2-FP8_audit.md（已更新为精确终值）。

## 全量模型校验 + pytest 测试套件（turn 3）

对 model_configs 下全部 100 个模型目录做了构建扫描 + 真值校验。分类：llm 49 / diffusers 11 / multimodal 36 / unknown 4，构建错误 0。

### 本轮又修的 3 个 bug（serve_model_arch.py）
1. **视频 diffusers patch_size 崩溃**：`Robbyant__lingbot-video-moe-30b-a3b` 的 `patch_size` 是 list `[t,h,w]`，`int()` 直接抛异常。新增 `scalar_int()` helper（list 取最后一维=空间维），line 1927 改用它。
2. **MiniMax 专家数漏读**：MiniMax-M2.7 用 `num_local_experts=256`（非 n_routed_experts/num_experts），原来识别为 0 专家 → 当稠密算成 4.84B（真值 ~230B）。给 num_experts 别名链加上 `num_local_experts`。修正后 228.64B/active 10.37B，与公开 MiniMax-M2(~230B/10B) 吻合。
3. **重构**：把估算数学抽成独立纯函数 `parse_llm_dims(config, params_config)` + `estimate_llm_metrics(dims)`（返回 total/active/kv_cache_mb_per_1k/gflops + 组件分解），build_llm_payload 改为调用它。UI 与测试共用同一真值源，行为不变（已验证 GLM/Qwen 输出一致）。

### 真值校验结论（bf16 字节代理：bytes ≈ 2×params）
非量化模型全部 ≤2%（Qwen 全家 ~0%、GLM-5.1/5.2 -1.4%、Hy3 -1.8%、Mellum/VibeThinker/MiniCPM5 ~0%）。可解释离群：Qwen3-Embedding-8B +8.2%（embedding 模型去掉 lm_head）、openPangu -8.6%（checkpoint 含额外 MTP 层）。大偏差全部是 fp8/int8/fp4/compressed 量化 checkpoint（字节≠2×参数，代理不适用），已按 quant 标记排除严格校验。

### pytest 测试套件（tests/，247 项全过，1.75s）
- test_unit_estimation.py(20)：白盒合成 config——scalar_int、dense GQA、q_dim≠hidden、tie_word_embeddings、MoE 用 moe_intermediate_size、first_k_dense 拆分、专家数别名(n_routed/num_experts/num_local_experts)、LongCat 别名、MLA 注意力+latent KV、guard rails。
- test_build_all_models.py(201)：每个模型目录构建不报错 + payload 结构断言。
- test_llm_ground_truth.py(13)：非量化 LLM 参数量 vs safetensors total_size，默认容差 5%，Qwen3-Embedding/openPangu 记为已知例外(12%)。量化 checkpoint 按 config flag 或目录名(fp8/int8/fp4/awq/gguf/…)排除。
- test_golden_metrics.py(13)：12 个锚点模型冻结精确 total/active/kv/gflops 值防回归。
运行：项目根目录 `python -m pytest`（已配 pytest.ini）。pytest 装在 C:\Users\tzh\.workbuddy\binaries\python\envs\default。

### 关键约定（供后续复用）
- 判断 checkpoint 是否量化：config.quantization_config 存在，或 torch_dtype/dtype 含 fp8/int8/fp4，或目录名含 fp8/fp4/int8/int4/w8a8/w4a16/awq/gptq/gguf/nf4/mxfp4/bnb。LongCat-INT8 的 config 无 quantization_config，只能靠目录名识别。
- safetensors 的 metadata.total_size 是参数字节真值；仅 bf16/fp16 非量化模型才满足 bytes=2×params。

### 真实架构要点（config.json）
hidden=6144, layers=78, heads=64, q_lora=2048, kv_lora=512, qk_rope=64, qk_nope=192, v_head=256, intermediate=12288, moe_inter=2048, n_routed=256, n_shared=1, top=8, first_k_dense=3, vocab=154880。含 DSA indexer(index_n_heads=32) 与 num_nextn_predict_layers=1(MTP)。专家在 76 层（layer 3..78），稠密层 0/1/2。

## 全量代码复审（turn 4，"是否还有其他计算错误"）

把非 LLM 路径（multimodal / diffusers）也通读了一遍，并写了诊断脚本扫描所有 diffusers/multimodal 的推导公式输出。

### 修了一个真实的分类 bug：classify_model_dir
- `google__diffusiongemma-26B-A4B-it` 同时有 `model_index.json` 和 `config.json`。旧逻辑只要存在 model_index.json 就判为 diffusers，但 diffusiongemma 的 model_index 只有 `model/processor/scheduler` 键（不是真正的 diffusers 组件目录），导致它被当成 diffusers 构建出 transformer_width=0、latent_tokens=0 的空壳图。
- 修正：要求 model_index.json 至少含一个实质性 diffusers 组件键（transformer/unet/vae/text_encoder/controlnet/mllm），否则 fall-through 到 LLM/multimodal 分支。注意 `scheduler`/`tokenizer` 故意排除——太泛，很多非 diffusers checkpoint 也有。
- 修正后 diffusiongemma 走真实 config.json 解析（语言隐藏 2816、主干 30 层等真实值）。分类分布变为 llm 50 / diffusers 9 / multimodal 37 / unknown 4（原先 diffusers 11 含了这个误判）。
- 新增 tests/test_classify.py(15)：真实 diffusers 仍判 diffusers、model_index 无实质组件不算 diffusers、diffusiongemma 走真实 config、组件键集不含 scheduler/tokenizer、每个 model_index 目录 self-consistent。

### 其余路径经审查：计算正确，无其它 bug
- derive_vae_scale：SDXL/FLUX 等 block_out_channels=[128,256,512,512]→8；dim_mult=[1,2,4,4]→8；默认 8。与主流 VAE 8× 下采样一致。
- infer_transformer_width：hidden_size→heads*head_dim→in_channels→text_hidden 回退链正确；video patch_size 经 scalar_int 取空间维正确。
- multimodal token 计数：image_token_count=ceil(patches/(merge²))、video 含 temporal_patch、total_tokens=text+image+video，公式与显示一致；image_patch_width=3·patch² 正确。
- LLM KV cache（非 MLA GQA）: 2·L·kv_heads·head_dim·2B（K+V, fp16）正确；FLOPs=2·active_params/1e9 是标准前向乘加估算，注释用语略有歧义但数值约定一致。
- 唯一"语义"瑕疵（未改）：multimodal 的 total_tokens 默认就把 image/video token 计入主干序列长度（解释图用默认分辨率），纯文本查询会高估；属展示选择，非数值错。

### 测试套件最终规模
262 项全过（test_unit_estimation 20 / test_build_all_models 201 / test_llm_ground_truth 13 / test_golden_metrics 13 / test_classify 15）。pytest 在 C:\Users\tzh\.workbuddy\binaries\python\envs\default。
